{
  "name": "@qf-jarvis/governed-memory-foundation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Policy-only foundation for structured Jarvis long-term memory. It creates no database, transport or background writer and keeps durable memory disabled until an exact owner-approved lifecycle policy is supplied.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "typecheck": "tsc --build tsconfig.build.json",
    "typecheck:tests": "tsc --noEmit -p tsconfig.json"
  }
}
