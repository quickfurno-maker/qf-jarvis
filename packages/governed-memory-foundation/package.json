{
  "name": "@qf-jarvis/governed-memory-foundation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Governed long-term memory admission/runtime over canonical AgentMemoryRecordV1 and MemoryInvalidationRequestV1. Durable reads/writes are default-off until an exact owner-approved lifecycle policy is supplied; invalidation remains available for cleanup. No database or transport implementation lives here.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "typecheck": "tsc --build tsconfig.build.json",
    "typecheck:tests": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@qf-jarvis/contracts": "workspace:*"
  }
}
