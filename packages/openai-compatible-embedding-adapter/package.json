{
  "name": "@qf-jarvis/openai-compatible-embedding-adapter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Fail-closed OpenAI-compatible embeddings adapter for the governed Jarvis knowledge index. Injected URL/token/fetch only; no env reads, retries, fallback or logging.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "typecheck": "tsc --build tsconfig.build.json",
    "typecheck:tests": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@qf-jarvis/knowledge-index": "workspace:*"
  }
}
